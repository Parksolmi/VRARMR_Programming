using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Print : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("VR/AR/MR 프로그래밍");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
